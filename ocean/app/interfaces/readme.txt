Interfaces are used in OCEAN to ensure the implementation is well
structured. The Protocol superclass is used to enforce type hinting
when running mypy and the ABC superclass is used to enforce the
existence of abstract methods when running the program.